const buttons = document.querySelectorAll(
  ".d-table.table-fixed.col-12.width-full.py-4.border-bottom.color-border-muted .btn.btn-sm"
);

for (let i = 0; i < buttons.length; i++) {
  buttons[i].click();
}
